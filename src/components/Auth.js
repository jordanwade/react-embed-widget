import React from 'react';

const Auth = (props) => {
  return (
    <div>
      <p>This is a Auth component/widget for {props.church} church</p>
    </div>
  );
};

Auth.propTypes = {};

export default Auth;
